if(AIF_BUILD_PLUGINS)
add_executable(aif_field_benchmark benchmark.cpp)
target_include_directories(aif_field_benchmark PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}")
target_link_libraries(aif_field_benchmark PRIVATE ${CMAKE_DL_LIBS})
aif_target_defaults(aif_field_benchmark)
add_dependencies(aif_field_benchmark aif_field_plugin)
endif()
