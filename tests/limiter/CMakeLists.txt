if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
 add_executable(aif_limiter_filter_tests filter_tests.cpp)
 target_include_directories(aif_limiter_filter_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}" "${PROJECT_SOURCE_DIR}/src/limiter/include")
 target_link_libraries(aif_limiter_filter_tests PRIVATE ${CMAKE_DL_LIBS})
 aif_target_defaults(aif_limiter_filter_tests)
 add_dependencies(aif_limiter_filter_tests aif_limiter_plugin)
 add_test(NAME limiter.filters COMMAND aif_limiter_filter_tests "${AIF_TEST_RUNTIME}" "$<TARGET_FILE:aif_limiter_plugin>")
endif()

add_executable(aif_limiter_kernel_tests kernel_tests.cpp)
target_link_libraries(aif_limiter_kernel_tests PRIVATE AvsFilters::LimiterKernels)
aif_target_defaults(aif_limiter_kernel_tests)
add_test(NAME limiter.kernels COMMAND aif_limiter_kernel_tests)
add_executable(aif_limiter_c_consumer c_consumer.c)
target_link_libraries(aif_limiter_c_consumer PRIVATE AvsFilters::LimiterKernels)
add_test(NAME limiter.c_consumer COMMAND aif_limiter_c_consumer)
