if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
 add_executable(aif_mask_filter_tests filter_tests.cpp)
 target_include_directories(aif_mask_filter_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}" "${PROJECT_SOURCE_DIR}/src/mask/include")
 target_link_libraries(aif_mask_filter_tests PRIVATE ${CMAKE_DL_LIBS})
 aif_target_defaults(aif_mask_filter_tests)
 add_dependencies(aif_mask_filter_tests aif_mask_plugin)
 add_test(NAME mask.filters COMMAND aif_mask_filter_tests "${AIF_TEST_RUNTIME}" "$<TARGET_FILE:aif_mask_plugin>")
endif()

add_executable(aif_mask_kernel_tests kernel_tests.cpp)
target_link_libraries(aif_mask_kernel_tests PRIVATE AvsFilters::MaskKernels)
aif_target_defaults(aif_mask_kernel_tests)
add_test(NAME mask.kernels COMMAND aif_mask_kernel_tests)
add_executable(aif_mask_c_consumer c_consumer.c)
target_link_libraries(aif_mask_c_consumer PRIVATE AvsFilters::MaskKernels)
add_test(NAME mask.c_consumer COMMAND aif_mask_c_consumer)
