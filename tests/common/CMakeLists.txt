if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
  add_executable(aif_host_cpu_runtime_tests host_cpu_runtime_tests.cpp)
  target_include_directories(aif_host_cpu_runtime_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}")
  target_link_libraries(aif_host_cpu_runtime_tests PRIVATE ${CMAKE_DL_LIBS})
  aif_target_defaults(aif_host_cpu_runtime_tests)
  set(cpu_test_plugins)
  foreach(family focus rotation crop planes channel_display rgb_merge stack rows_columns color_adjust convolution merge greyscale invert limiter blank_clip color_bars)
    add_dependencies(aif_host_cpu_runtime_tests aif_${family}_plugin)
    list(APPEND cpu_test_plugins "$<TARGET_FILE:aif_${family}_plugin>")
  endforeach()
  add_test(NAME common.host_cpu_runtime COMMAND aif_host_cpu_runtime_tests "${AIF_TEST_RUNTIME}" ${cpu_test_plugins})
endif()
