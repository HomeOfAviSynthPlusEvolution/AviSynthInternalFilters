add_executable(aif_crop_kernel_tests kernel_tests.cpp)
target_link_libraries(aif_crop_kernel_tests PRIVATE AvsFilters::CropKernels)
aif_target_defaults(aif_crop_kernel_tests)
add_test(NAME crop.kernels COMMAND aif_crop_kernel_tests)
if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
 add_executable(aif_crop_filter_tests filter_tests.cpp)
 target_include_directories(aif_crop_filter_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}" "${PROJECT_SOURCE_DIR}/src/crop/include")
 target_link_libraries(aif_crop_filter_tests PRIVATE ${CMAKE_DL_LIBS})
 aif_target_defaults(aif_crop_filter_tests)
 add_dependencies(aif_crop_filter_tests aif_crop_plugin)
 add_test(NAME crop.filters COMMAND aif_crop_filter_tests "${AIF_TEST_RUNTIME}" "$<TARGET_FILE:aif_crop_plugin>")
endif()

add_executable(aif_crop_c_consumer c_consumer.c)
target_link_libraries(aif_crop_c_consumer PRIVATE AvsFilters::CropKernels)
add_test(NAME crop.c_consumer COMMAND aif_crop_c_consumer)
