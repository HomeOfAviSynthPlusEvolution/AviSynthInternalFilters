if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
 add_executable(aif_planes_filter_tests filter_tests.cpp)
 target_include_directories(aif_planes_filter_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}" "${PROJECT_SOURCE_DIR}/src/planes/include")
 target_link_libraries(aif_planes_filter_tests PRIVATE ${CMAKE_DL_LIBS})
 aif_target_defaults(aif_planes_filter_tests)
 add_dependencies(aif_planes_filter_tests aif_planes_plugin)
 add_test(NAME planes.filters COMMAND aif_planes_filter_tests "${AIF_TEST_RUNTIME}" "$<TARGET_FILE:aif_planes_plugin>")
endif()

add_executable(aif_planes_kernel_tests kernel_tests.cpp)
target_link_libraries(aif_planes_kernel_tests PRIVATE AvsFilters::PlanesKernels)
aif_target_defaults(aif_planes_kernel_tests)
add_test(NAME planes.kernels COMMAND aif_planes_kernel_tests)
add_executable(aif_planes_c_consumer c_consumer.c)
target_link_libraries(aif_planes_c_consumer PRIVATE AvsFilters::PlanesKernels)
add_test(NAME planes.c_consumer COMMAND aif_planes_c_consumer)
