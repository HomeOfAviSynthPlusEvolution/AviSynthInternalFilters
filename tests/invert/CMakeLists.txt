if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
 add_executable(aif_invert_filter_tests filter_tests.cpp)
 target_include_directories(aif_invert_filter_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}" "${PROJECT_SOURCE_DIR}/src/invert/include")
 target_link_libraries(aif_invert_filter_tests PRIVATE ${CMAKE_DL_LIBS})
 aif_target_defaults(aif_invert_filter_tests)
 add_dependencies(aif_invert_filter_tests aif_invert_plugin)
 add_test(NAME invert.filters COMMAND aif_invert_filter_tests "${AIF_TEST_RUNTIME}" "$<TARGET_FILE:aif_invert_plugin>")
endif()

add_executable(aif_invert_kernel_tests kernel_tests.cpp)
target_link_libraries(aif_invert_kernel_tests PRIVATE AvsFilters::InvertKernels)
aif_target_defaults(aif_invert_kernel_tests)
add_test(NAME invert.kernels COMMAND aif_invert_kernel_tests)
add_executable(aif_invert_c_consumer c_consumer.c)
target_link_libraries(aif_invert_c_consumer PRIVATE AvsFilters::InvertKernels)
add_test(NAME invert.c_consumer COMMAND aif_invert_c_consumer)
