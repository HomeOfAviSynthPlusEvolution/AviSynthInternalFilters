add_executable(aif_rotation_kernel_tests kernel_tests.cpp)
target_link_libraries(aif_rotation_kernel_tests PRIVATE AvsFilters::RotationKernels)
aif_target_defaults(aif_rotation_kernel_tests)
add_test(NAME rotation.kernels COMMAND aif_rotation_kernel_tests)
if(AIF_BUILD_PLUGINS AND AIF_TEST_RUNTIME)
 add_executable(aif_rotation_filter_tests filter_tests.cpp)
 target_include_directories(aif_rotation_filter_tests PRIVATE "${AIF_AVISYNTH_INCLUDE_DIR}" "${PROJECT_SOURCE_DIR}/src/rotation/include")
 target_link_libraries(aif_rotation_filter_tests PRIVATE ${CMAKE_DL_LIBS})
 aif_target_defaults(aif_rotation_filter_tests)
 add_dependencies(aif_rotation_filter_tests aif_rotation_plugin)
 add_test(NAME rotation.filters COMMAND aif_rotation_filter_tests "${AIF_TEST_RUNTIME}" "$<TARGET_FILE:aif_rotation_plugin>")
endif()

add_executable(aif_rotation_c_consumer c_consumer.c)
target_link_libraries(aif_rotation_c_consumer PRIVATE AvsFilters::RotationKernels)
add_test(NAME rotation.c_consumer COMMAND aif_rotation_c_consumer)
